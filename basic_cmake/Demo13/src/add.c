#include <stdio.h>
#include "add.h"

// 定义一个简单的函数
int add(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}
// 定义一个简单的函数
int add1(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}
// 定义一个简单的函数
int add2(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}
// 定义一个简单的函数
int add3(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}
// 定义一个简单的函数
int add4(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}
// 定义一个简单的函数
int add5(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}
// 定义一个简单的函数
int add6(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add7(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add8(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add9(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add10(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add11(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add12(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add13(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add14(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add15(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add16(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
} // 定义一个简单的函数
int add17(int a, int b, int d) {
  int c = a + b + d;
  printf("%d!\n", c);
  return c;
}